function head(x){

    return +(x.slice(0,1))
    }
    function tail(x){
    return (x.slice(1))
    }
    function init(x){
    return (x.slice(0,arr.length-1))
    }
    function last(x){
    return +(x.slice(arr.length-1))
    }